{
  "name": "ulp-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright E2E tests for ULP / SCMCube",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.11.0"
  }
}
