# tools/

Developer tools, code generators, scaffolding scripts.

Expected contents (added as needed):

- Module scaffolder — generate the 4-layer skeleton for a new module
- Plugin scaffolder — generate a new country plugin shell
- DTO sync — keep TypeScript DTOs in `frontend/ulp-shared-lib` in sync with .NET contracts
- Realm exporter — export Keycloak realm to `infra/docker/_dev/keycloak/import/`
- Schema diff — compare generated EF migrations against `ulpReq/ULP_DBD_v2.0_Schema.sql`
